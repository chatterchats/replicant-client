{
  "name": "@replicant/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-sidecar.mjs && tauri dev",
    "build": "node scripts/prepare-sidecar.mjs --release && tauri build",
    "check": "node --test scripts/*.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
