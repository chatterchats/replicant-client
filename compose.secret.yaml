services:
  replicantd:
    environment:
      RS_API_TOKEN: ""
      RS_API_TOKEN_FILE: /run/secrets/rs_api_token
    secrets:
      - rs_api_token

secrets:
  rs_api_token:
    file: ${RS_API_TOKEN_FILE_HOST:?set RS_API_TOKEN_FILE_HOST to the token file path}
